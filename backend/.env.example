# Server
PORT=5000
NODE_ENV=development

# MongoDB
MONGODB_URI=

# JWT
JWT_SECRET=
JWT_EXPIRE=

# Admin default (optional in dev)
ADMIN_EMAIL=
ADMIN_PASSWORD=

# CORS (comma-separated origins)
ALLOWED_ORIGINS=

# Google OAuth (for "Continue with Google"). Same value as frontend NEXT_PUBLIC_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_ID=
